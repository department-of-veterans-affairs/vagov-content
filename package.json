{
  "scripts": {
    "preinstall": "git clone --single-branch interim-cms --depth 1 https://github.com/department-of-veterans-affairs/vets-website ../vagov-apps",
    "install": "cd ../vagov-apps && yarn install",
    "start": "cd ../vagov-apps && npm run build"
  }
}
