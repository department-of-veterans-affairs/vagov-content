{
  "name": "vagov-content",
  "version": "0.0.1",
  "description": "Content files for VA.gov",
  "engines": {
    "node": "8.10.0",
    "yarn": "1.5.1"
  },
  "scripts": {
    "heroku-postbuild": "git clone --single-branch interim-cms --depth 1 https://github.com/department-of-veterans-affairs/vets-website ../vagov-apps && cd ../vagov-apps && npm install && npm run heroku-postbuild"
  }
}
